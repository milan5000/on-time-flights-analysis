[project]
name = "on-time-flights-analysis-utils"
version = "0.1.0"
description = "Various Python tooling for the repo"
readme = "../README.md"
requires-python = ">=3.14"
dependencies = [
    "polars>=1.39.3",
]
