apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: final-project-redis-pvc-test
  namespace: connorfeng
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: cinder-csi
  resources:
    requests:
      storage: 1Gi